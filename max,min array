#include<iostream>

using namespace std;

int main() {
int n,arr[n],i;
cin>>n;
for(i=0;i<n;i++){
cin>>arr[i];
}
int INT_MIN,INT_MAX;
int maxNo =INT_MIN;
int minNo =INT_MAX;

for(i=0;i<n;i++){
maxNo=max(maxNo,arr[i]);
minNo=min(minNo,arr[i]);
}
cout<<maxNo<<" "<<minNo;
return 0;
}
